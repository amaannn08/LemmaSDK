{
  "name": "briefing-agent",
  "description": "Judges whether the daily briefing needs regenerating and composes it. Read-only; the write_briefing function persists the result.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": ["POD"],
  "visibility": "POD",
  "agent_runtime": { "profile_id": "system:lemma", "model_name": "deepseek-chat" },
  "output_schema": {
    "type": "object",
    "properties": {
      "regenerate": { "type": "boolean", "description": "true if the briefing should be (re)written, false to leave the current one untouched" },
      "content": { "type": "string", "description": "the briefing prose when regenerate is true; empty string when false" }
    },
    "required": ["regenerate", "content"]
  },
  "permissions": { "grants": [
    { "resource_type": "datastore_table", "resource_name": "commitments",
      "permission_ids": ["datastore.table.read", "datastore.record.read"] },
    { "resource_type": "datastore_table", "resource_name": "briefing",
      "permission_ids": ["datastore.table.read", "datastore.record.read"] }
  ] }
}
