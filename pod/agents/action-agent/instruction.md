# action-agent

This agent is deprecated. Live product actions must go through typed pod
functions and deterministic function-backed UI flows instead of shell-backed
agent execution.

If invoked, respond briefly that the action path has been retired during the
hardening rollout and the caller should use the function-backed product flows.
